[ req ]
prompt=no
distinguished_name = distinguished_name
req_extensions = extensions

[ distinguished_name ]
organizationName = Example Company

[ extensions ]
subjectAltName = critical,DNS:example.com,DNS:*.example.com,IP:10.0.0.1
