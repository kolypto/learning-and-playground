apiVersion: 1

datasources:
 # Get data from Prometheus
 - name: prometheus
   type: prometheus
   url: http://prometheus:9090
   access: proxy
   orgId: 1
   version: 1
   isDefault: true
   editable: false
